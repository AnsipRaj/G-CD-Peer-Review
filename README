This document is aimed at providing an overview of the files that are present in this repo

The repository contains the following files 

1. run_analysis.R - File that contains the R script that was used to generate tidy data from the UCI HAR Dataset. The script generated two datasets. The firstset.txt is a tidy dataset that contains the raw values of the variables mentioned in the codebook. The tidyset.txt is the tidy dataset that is required for the course. 
2. tidyset.txt- This contains the dataset that is specified in Q5 of the assignement. It contains the mean for each variable for each subject and each activity
3. CodeBook.md- A markdown file that contains information on the variables in the tidy dataset and the transformation steps that were used to generate it. 

To generate the tidy dataset one can run the run_analysis.R script. 
